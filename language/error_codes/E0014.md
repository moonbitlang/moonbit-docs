# E0014

Alerts are special warnings that indicate the usage of API marked with the
[`#internal` attribute](../attributes.md#internal-attribute).

#### NOTE
Currently, MoonBit won't emit this error code. See [alert warning](../../toolchain/moon/package.md#alert-warning)
for how alerts are reported and configured.

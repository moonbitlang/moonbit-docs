块与语句

块是一个表达式，它包含一系列语句和一个可选的表达式。

{
  statement1
  statement2
  expression
}

例如，上面的代码会依次执行 statement1、statement2，并对 expression 进行求值。该块的求值结果就是最后那个表达式的求值结果。如果省略了最后的表达式，则该块的结果为 ()，其类型为 Unit。

一个语句可以是变量声明、变量赋值，或者任何返回类型为 Unit 的表达式。

块还与一个命名空间作用域相关联。在主程序中，内部块中声明的变量 a 会遮蔽外部的 a，该变量仅在内部块中可见。

# E0089

Warning name: `guard_redundant_else`

The condition or pattern of a `guard` is exhaustive, so its `else` clause can
never run.

## Erroneous example

```{literalinclude} /sources/error_codes/0089_error/top.mbt
:language: moonbit
```

## Suggestion

Remove the unreachable `else` clause.

```{literalinclude} /sources/error_codes/0089_fixed/top.mbt
:language: moonbit
```

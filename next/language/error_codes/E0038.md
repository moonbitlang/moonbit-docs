# E0038

Warning name: `missing_invariant`

## Erroneous example

The for loop does not have `where` clause containing `invariant`.

```{literalinclude} /sources/error_codes/0038_error/top.mbt
:language: moonbit
```

## Suggestion

Add some invariants.

```{literalinclude} /sources/error_codes/0038_fixed/top.mbt
:language: moonbit
```

# E0047

Invalid mbti file.

This warning is emitted when the compiler encounters an invalid `.mbti` file.
Regenerate the `.mbti` file (or fix the tool that produced it) so that the
interface matches the current compiler version.


# E0074

Warning name: `missing_doc`

Public declaration is missing documentation.

When documentation checking is enabled, public declarations should have a
docstring starting with `///`. This keeps generated API documentation useful for
downstream users.

## Erroneous example

```{literalinclude} /sources/error_codes/0074_error/top.mbt
:language: moonbit
```

## Suggestion

Add a docstring above the public declaration.

```{literalinclude} /sources/error_codes/0074_fixed/top.mbt
:language: moonbit
```

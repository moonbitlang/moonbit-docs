{
  "name": "icy-fog-9a56",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "start": "moon build --target wasm-gc && wrangler dev",
    "test": "vitest"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.1.19",
    "@cloudflare/workers-types": "^4.20240620.0",
    "typescript": "^5.5.3",
    "vitest": "1.3.0",
    "wrangler": "^3.63.1"
  }
}